<div class="container">
	<div class="row">

<%= flash[:success] %>
<%= render 'books/list', user: @user , book: @book %>

<div class="col-xs-9">
<h2>Books</h2>

<table class="table table-hover table-inverse">
	<thead>
		<tr>
			<th></th>
			<th>Title</th>
			<th>body</th>
		</tr>
	</thead>
	<tbody>
    <% @books.each do |book| %>
		<tr>
			<td><%= attachment_image_tag book.user, :image,size: "100x100" %></td>
			<td><%= link_to book.title , book_path(book.id) , class: "edit_book_#{book.id}" %></td>
			<td><%= book.opinion %></td>
		</tr>
      <% end %>
	</tbody>
 </table>

 </div>


 <div class="container">
  	<div class="row">
    	<div class="col-xs-3 col-xs-offset-9">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>

    </div>
  </div>